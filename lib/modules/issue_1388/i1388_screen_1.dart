import 'package:flutter/material.dart';

class I1388Screen1 extends StatelessWidget {
  const I1388Screen1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        alignment: Alignment.center,
        child: Text('I1388 screen 1'),
      ),
    );
  }
}
